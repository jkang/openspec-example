@e2e
Feature: 数据持久化（JSON 文件落盘）与进程级重启恢复
  以 STORAGE=file 拉起 Node 后端（独立临时 data 目录），验证写操作真实落盘、
  进程级重启后会话凭证与历史订单可恢复。由 ./init.sh e2e:persist 编排
  （steps 自行 spawn/重启后端进程，不依赖 Vue 前端与 3000 端口）。

  @persist
  Scenario: 注册登录下单支付后进程重启，登录态与订单保持
    Given 文件存储后端已启动（临时数据目录）
    When 买家注册新账号并自动登录
    And 买家将商品加入购物车并完成下单与支付
    Then 数据文件已真实落盘（用户、会话、订单文件均含对应记录）
    When 后端进程停止并再次启动（同一数据目录）
    Then 原会话凭证访问我的订单仍返回该订单
    And 订单状态与重启前一致（已支付）
    And 注册用户可用原手机号重新登录
