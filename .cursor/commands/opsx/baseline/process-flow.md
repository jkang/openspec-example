---
description: 维护业务基线中的 Core Business Process Flow 文档
---

# /opsx:baseline/process-flow

维护 `docs/baseline/business_process.html`，记录 L1/L2/L3 分层业务流程。

## 核心动作
1. 调用 `openspec-baseline-process-flow` 技能。
2. 维护 L1 (价值流)、L2 (协同流) 和 L3 (规则流)。
3. 直接回写 HTML 中的结构化数据，确保 ID 锚点稳定。
