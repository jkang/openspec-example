# ref-palette-slide — optional config (no credentials required)

# Font used for all runs (latin + east-asian).
SLIDE_FONT=PingFang SC

# Optional: explicit LibreOffice binary for render_slide.sh
SOFFICE_BIN=
