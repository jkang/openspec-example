---
name: ref-palette-slide
description: |
  从一张参考图（封面 / 收尾页 / 品牌页）抽取配色并沉淀为「PPT 内容页配色要求」，
  再用这套配色把任意内容（图片 / 文字 / 大纲）生成一页 16:9 的 PPTX，并做几何自检。

  Triggers when user mentions:
  - "参考这张图的配色做一页PPT"
  - "提取配色" / "解读配色" / "配色要求"
  - "把这张图做成一页PPT" / "单页PPT生成"
  - "16:9 PPT" / "生成一页 PPT"
  - "ref palette slide" / "palette to slide"
---

# ref-palette-slide

把「参考图配色」→「配色规范」→「一页 16:9 PPT」这条链路固化成可复用流程。

适用场景：拿到一张品牌页 / 封面 / 收尾页作为**配色来源**，另有一张信息图 / 一段大纲作为**内容来源**，
需要输出一张配色统一、可直接放进演讲的内容页。

---

## 设计系统（v2）— 先读这一节

这套 skill 的产出以**可读性优先**，不以"塞得下"为荣。三条硬约束：

### 1. 字号下限 10pt（硬门禁）

`MIN_PT = 10.0`，在 `add_text()` 里**统一强制**——所有 layout、chip、图标、页脚都继承，
没有任何地方能绕过。生成后用 `--check` 会逐 run 扫描并报出违规：

```bash
python3 scripts/build_slide.py --content c.json --out s.pptx --check
# -> checks OK (inside 16:9 canvas; every run >= 10pt)
```

**推论：内容装不下时，砍内容，不要缩字号。** 这是本 skill 最重要的一条纪律。

### 2. 密度预算

10pt 下限意味着单页能承载的信息量是有限的。预算表：

| 约束 | 值 | 说明 |
| :--- | :--- | :--- |
| 单页文字层级 | ≤ 3 层 | 例如：标题 / 正文 / 注释。不要再加第四层 |
| 单个列表项 | ≤ 4 条 | `MAX_LIST = 4`。第 5 条起请拆页 |
| 单列文字块 | ≤ 2 个 | 一列里最多"一段说明 + 一个结论" |
| 单个文本块 | ≤ 3 行 | 超过就说明这句话该被砍掉一半 |
| 表格单元格 | ≤ 12 字 | 单元格是索引，不是句子 |

### 3. 视觉语言

- **无装饰条**：卡片顶部不加色条；标题下不加装饰横线。
- **无嵌套盒子**：面板里不再套盒子。强调靠 **tint + 字号**，不靠描边。
- **一处一色块**：一页里只允许一个"实色大色块"承担结论（如底部结论条）。
- **示意图不套面板**：浅色 tint 元素直接浮在卡片白底上。

### 4. 字号阶梯（可直接抄）

| 元素 | 字号 |
| :--- | :--- |
| 页面主标题 | 26–28pt Bold |
| 副标题 | 11pt |
| 区块/团队名 | 15–19pt Bold |
| 主体正文 | 10.5–11.5pt |
| 注释 / 标签 / 页脚 | **10pt**（下限） |
| 数字强调（柱状图数值） | 16pt Bold |

---

## Quick Usage（已配置）

```bash
# 1) 从参考图抽取配色，产出 token
python3 scripts/extract_palette.py cover.png --colors 8 --out mypalette.json

# 2) 用 token + 内容生成一页 16:9 PPTX（内含几何自检）
#    模板 A：流程 + 对比看板
python3 scripts/build_slide.py \
  --content assets/content.example.json \
  --palette assets/palette.nidd-citylab.json \
  --out out/slide.pptx --check

#    模板 B：五列痛点卡（含内置示意图）
python3 scripts/build_slide.py \
  --content assets/content.pain-cards.example.json \
  --palette assets/palette.nidd-citylab.json \
  --out out/pain.pptx --check

#    模板 C：成熟度轨道 + 五维「现状 → 破局」
python3 scripts/build_slide.py \
  --content assets/content.breakthrough.example.json \
  --palette assets/palette.nidd-citylab.json \
  --out out/breakthrough.pptx --check

#    模板 D：分型标尺 + 团队卡（含企业示例）
python3 scripts/build_slide.py \
  --content assets/content.team-fit.example.json \
  --palette assets/palette.nidd-citylab.json \
  --out out/teamfit.pptx --check

#    模板 E：分层泳道蓝图（AGENT/FLOW/SKILL/COMMAND/OUTPUT）
python3 scripts/build_slide.py \
  --content assets/content.swimlane-l1.example.json \
  --palette assets/palette.nidd-citylab.json \
  --out out/layer1.pptx --check

#    模板 F：能力「上手 vs 落地」落差对比
python3 scripts/build_slide.py \
  --content assets/content.capability-gap.example.json \
  --palette assets/palette.nidd-citylab.json \
  --out out/capability.pptx --check

#    模板 G：引言 + 外部图占位 + 递进路径
python3 scripts/build_slide.py \
  --content assets/content.diagram-embed.example.json \
  --palette assets/palette.nidd-citylab.json \
  --out out/embed.pptx --check

# 3) 渲染成 PNG 做视觉 QA
./scripts/render_slide.sh out/slide.pptx out/
```

依赖：`python-pptx`、`Pillow`。渲染需要 LibreOffice（推荐）或 macOS + Keynote。

---

## Workflow

### Step 1 — 解读参考图配色（产出「配色要求」）

1. `extract_palette.py` 拿到主色 + 占比 + 角色建议。
2. **人工补一层语义解读**：源图里每种颜色"在表达什么"（品牌主色？动线？背景？）。
   脚本给的是数值，语义必须自己写。
3. 把结果整理成一份 `color-spec.md`（模板见 `assets/color-spec.md`），至少包含：
   - 核心色 token 表（HEX）
   - **语义约定**（一个颜色只表达一个含义）
   - **面积比例**（60/30/10）
   - 文字与对比度规则
   - 组件级配色表
   - 禁止事项（源图没有的颜色一律不许引入）

> 关键判断：先看源图**有没有暖色**。没有暖色的品牌色板，内容页也绝不能引入红/橙/紫，
> 否则"串味"。负向信息改用深中性色（`slate`）+ 白字表达。

### Step 2 — 内容建模

把内容源（信息图/大纲）拆成结构化 JSON，字段见 `assets/content.example.json`：

- `title_lead` / `title_accent`：标题拆两段，**accent 段用副色（蓝）** 承担焦点。
- `stages[]`：流程节点。每个节点含 `name` / `desc` / `level` / `cap` / `bar`(色条) / `icon`。
  - `level` 走 **成熟度色阶**（灰→绿单向渐变）。
  - 瓶颈节点用 `tag` + `border`（蓝）标出。
  - 终态节点用 `fill: green_dark` + 白字。
- `boards[]`：对比看板（如"过去 vs 现在"）。每个 `column` 含 `name` / `count` / `items[]` / `accent`。
  - `accent` 只从 `slate | blue | green_soft | green_tint` 里选。

### Step 3 — 生成 + 自检

```bash
python3 scripts/build_slide.py --content <content.json> --palette <palette.json> --out slide.pptx --check
```

`--check` 做**几何自检**：所有形状是否越出 16:9 画布。先过这一关再渲染。

### Step 4 — 视觉 QA（必做，但成本高）

```bash
./scripts/render_slide.sh slide.pptx out/
```

然后**用子 agent 看渲染图**（不要自己看，你会看到你期望的东西）：

```
Assume there are issues — find them. 检查：元素重叠、文字溢出/被裁、
装饰线是按单行文本定位的但标题换行了、间距不均、边距 < 0.5"、
列宽不一致、低对比度文字、占位符残留。
```

至少完成一轮「发现问题 → 修复 → 复验」。

---

## 布局模板说明

`build_slide.py` 内置七套单页模板（画布固定 13.333" × 7.5"），由内容 JSON 的
`layout` 字段选择。**改版式时只改 `draw_*` 里的坐标常量，不要动 `PALETTE` 的语义分工。**

### `layout: "pipeline-boards"`（默认）

适合：端到端流程 + 前后对比。示例：`assets/content.example.json`

```
┌───────────────────────────────────────────────────────────────────┐
│ 编码自动化了，需求还是手工作坊                        28pt        │  0.28–0.88
│ 副标题 11pt                                                       │  0.94–1.26
├───────────────────────────────────────────────────────────────────┤
│ ⬤Idea  ⬤需求分析  ⬤设计  ⬤编码  ⬤测试  ⬤部署  ⬤运营  ⬤价值实现    │  1.34–2.58
│         [L0~L1]  [L2]  [L3~L4] [L3~L4] [L4~L5] [L4~L5]           │
│         ↑ 只保留「图标 + 名称 + 等级徽章」三层，desc/caption 全部砍掉│
├─────────────────────────────┬─────────────────────────────────────┤
│ 过去：需求 backlog 做不完    │ 现在：开发在等需求                  │  2.76–7.04
│                             │                                     │
│   52   18   15   12   8     │   38   27   12   6   4   3          │  ← 16pt 数值
│   ██   ▄    ▄    ▄    ▂     │   ██   █    ▄    ▂   ▂   ▂          │  ← 柱高 ∝ 数量
│  需求  设计  开发  测试  上线 │  Idea  需求  设计  开发 测试 上线    │  ← 10pt 列名
│                             │                                     │
│ ▌主要问题：backlog 积压严重  │ ▌主要问题：开发在等需求             │  6.36–6.92
└─────────────────────────────┴─────────────────────────────────────┘
```

关键字段：
- `stages[]`：`level` 走成熟度色阶；`tag`+`border` 标瓶颈；`icon` 见 `draw_icon`。
  **不再渲染 `desc` / `cap`** —— 8 列在 10pt 下放不下第三层文字。
- `boards[]`：每列只要 `name` + `count`。渲染成**柱状图**（柱高 ∝ 数量），
  而不是卡片堆叠。信息量骤降但"积压堵在哪一段"一眼可见，比一堆小卡片更有效。
  两块的柱高各自按本块峰值归一，看的是**形状**而不是绝对值。

### `layout: "pain-cards"`

适合：N 个并列痛点 / 能力项，每项需要一张示意图。示例：`assets/content.pain-cards.example.json`

```
┌───────────────────────────────────────────────────────────┐
│ 标题(lead+accent) 28pt                                    │  0.34–0.94
│ 副标题（关键词蓝色加粗 + 说明灰色）                        │  1.00–1.32
├──────────┬──────────┬──────────┬──────────┬───────────────┤
│ 01       │ 02       │ 03       │ 04       │ 05            │  1.74  30pt 浅蓝序号
│ 标题 15pt│ 标题     │ 标题     │ 标题     │ 标题          │  2.32–3.22
│ 描述10.5 │ 描述     │ 描述     │ 描述     │ 描述          │  3.30–4.14
│  示意图  │  示意图  │  示意图  │  示意图  │  示意图        │  4.28–6.88
│ (无面板) │ (无面板) │ (无面板) │ (无面板) │ (无面板)      │  浮在卡片白底上
└──────────┴──────────┴──────────┴──────────┴───────────────┘
```

**设计意图：刻意"去模板化"**
- 卡片**没有描边、没有顶部装饰条**，只靠白色面 + 浅绿白底色的微差分层。
- 编号用**大号浅蓝数字 `01`–`05`**，不用实心圆形徽章。
- 示意图**不套面板**，浅色 tint 直接浮在卡片白底上；也**不套第二层框**。
- 字号整体放大（标题 13pt / 描述 8.5pt），留白优先。

关键字段：
- `cards[].illustration` 选内置示意图，`illus` 传该图所需参数。
- `subtitle_runs`（可选）替代 `subtitle`：结构是 `[[[文本, 覆盖属性], ...]]`，
  即"段落列表 → 段内 run 列表"，可做关键词加粗变色。
- `subtitle`（v2 起**不再渲染**）：卡片只保留「序号 → 标题 15pt → 描述 10.5pt → 示意图」
  三层。原来的 subtitle + 典型表现两层已删除，为 10pt 下限腾出空间。
- 示意图内部的列表一律取前 4 条（`labels[:4]`、`steps[:5]`、`bubbles[:3]`），
  超出的部分请在内容侧删掉，不要指望渲染器替你压缩。

| `illustration` | 语义 | `illus` 参数 |
| :--- | :--- | :--- |
### `layout: "breakthrough"`

适合：**"现状 → 破局"** 的转折页——左证问题、右给答案。示例：`assets/content.breakthrough.example.json`

```
┌──────────────────────────────────────────────────────────────────┐
│ 如果需求过程想要达到 L3（L3 用副色）                              │  0.32–0.90
│ 副标题：从 L0~L1 … 跨到 L3 …                                     │  0.96–1.28
│                                                                  │
│      ┌现在┐              ┌目标┐                                  │  1.32
│  ●───┴─●──┴───────◎──────┴────●─────●                           │  1.76  成熟度轨道
│  L0     L1      L2     L3     L4    L5                          │  1.98
├──────────────────────────────────────────────────────────────────┤
│ ⚙ 工具    ▸▸ 流程   ●● 组织   ▤ 知识   ☺ 人                      │  2.34
│ [现状]        [现状]      [现状]    [现状]    [现状]              │
│ 问题…         问题…       问题…     问题…     问题…              │
│      ↓             ↓           ↓         ↓        ↓             │
│ ┌────────┐   ┌────────┐  ┌────────┐ ...                         │  4.48–6.10
│ │[破局]  │   │[破局]  │  │[破局]  │        ← blue_tint 面板      │
│ │ 方案…  │   │ 方案…  │  │ 方案…  │                             │
│ └────────┘   └────────┘  └────────┘                             │
├──────────────────────────────────────────────────────────────────┤
│ ▌五个维度里，四个可以靠设计解决 —— 只有「人」，必须自己决定。▌     │  6.26–6.98
└──────────────────────────────────────────────────────────────────┘
```

关键字段：
- `scale.levels` / `scale.now{from,to,label}` / `scale.target{index,label}`：
  成熟度轨道。轨道**复用 `LEVEL_COLORS` 色阶**，"现在"段用色阶原色，
  "现在→目标"区间用蓝色填充表示"要补的差距"，目标之后（L4/L5）转灰表示"暂不在范围"。
  这一条轨道就是本页与前面"成熟度 L0–L5"那页的锚点。
- `columns[].icon ∈ gear | flow | people | doc | person`，`problem` / `solution` 各一段。
- `problem_label` / `solution_label`：两个小标签的文案，默认 `现状` / `破局`。
- `thesis`（可选）：底部整幅结论条。**只放一句话**，否则会抢走正文的注意力。

**设计意图**：`破局` 面板是本页唯一的"色块"，用 `blue_tint` 而不是描边盒子；
`现状` 完全不加底色，靠灰色文字退到背景。这样读者的视线会自然落在"答案"上。

---

### `layout: "team-fit"`

适合：**分型 → 匹配** 页。上半是一张"分类标尺"（N 个维度 × 3 档），
下半是若干张团队卡，每张卡给出**企业实例**并映射到一个方案层。
示例：`assets/content.team-fit.example.json`

```
┌──────────────────────────────────────────────────────────────────────┐
│ 三类流程，各归其位：先给团队分型，再选流程形态                        │  0.30–1.24
├──────────────────────────────────────────────────────────────────────┤
│            决策链路   协作界面   影响面      规则密度    生命周期      │  1.18
│ ▍轻 ▍  PM一人可定  1–2角色  内部可回滚  技术属性强  一次性           │  1.54  ← 浅绿行带
│ ▍中 ▍  融合拍板   3–4角色  单业务线    中等编排    持续迭代          │  1.98  ← 浅蓝行带
│ ▍重 ▍  分离会签   5+跨部门 资金/合规   密集跨域    长周期复用        │  2.42  ← 浅灰行带
├──────────────────────────────────────────────────────────────────────┤
│ ┌ L1·极简交付 ┐ ┌ L2·轻量产品 ┐ ┌ L3·业务产品 ┐                      │  3.00
│ │ 小型工具团队 │ │ 单业务线产品 │ │ 跨部门业务   │                    │
│ │ 一句话画像   │ │ 一句话画像   │ │ 一句话画像   │                    │
│ │ 企业示例     │ │ 企业示例     │ │ 企业示例     │                    │
│ │ [实例1][实例2][实例3]           ← 层色 tint 胶囊                  │
│ │ 平衡点：…    │ │ 平衡点：…    │ │ 平衡点：…    │                    │
│ └──────────────┘ └──────────────┘ └──────────────┘                   │
└──────────────────────────────────────────────────────────────────────┘
```

关键字段：
- `scale.rows[]`（`label` + `tone ∈ green|blue|slate`）× `scale.dimensions[].cells[]`（长度必须等于行数）。
- `teams[].tone` 决定整张卡的层色（chip / 实例胶囊 / 平衡点文字）。
- `teams[].examples[]` 建议 **2–3 条**，每条一句话、能在 1 行内读完；
  写成"谁做的什么系统"而不是抽象名词，否则失去"结合企业实际"的意义。
- `teams[].balance{label,value}`：这一层在优化什么（取自三力拉扯的平衡点）。

**设计意图**：`tone` 在"标尺行"和"团队卡"之间复用同一套绿/蓝/板岩，
让读者不需要连线就能看出"哪一档 → 哪一种团队 → 哪一类流程"。

---

### `layout: "swimlane"`

适合：**分层治理蓝图**——把「谁负责 / 做什么 / 用什么 skill / 敲什么命令 / 产出什么」
叠成泳道。示例：`assets/content.swimlane-l1| l2 | l3.example.json`
（转写自 `learning-sdd/visuals/workflow-blueprint.html` 的三张图）

```
┌──────────────────────────────────────────────────────────────────────┐
│ [01] 极简交付 · Layer 01                    [SPEC-DRIVEN DEVELOPMENT]│  0.28–1.04
│      Minimalist Delivery / Single Change Pipeline                    │
├──────────┬───────────────────────────────────────────────────────────┤
│  AGENT   │ ▓▓ ENGINEER AGENT (PRIMARY) ▓▓▓▓▓▓ │ LEAD (GOVERNANCE)  │  1.20
├──────────┼───────┬───────┬───────┬───────┬───────────────────────────┤
│  FLOW    │探索   │提案   │实施   │同步   │归档                       │  1.83–2.73
│          │       │       │       │       │  (block 型卡片 1.40 高)   │
│          │  ╰──── NEXT STORY ────┤        │                          │  3.46
├──────────┼───────┴───────┴───────┴───────┴───────────────────────────┤
│  SKILL   │explore│propose│apply-…│sync-… │archive-change            │  3.98
│ COMMAND  │/opsx:explore│/opsx:propose│/opsx:apply│/opsx:sync│…       │  4.85
├──────────┼───────────────────────────────────────────────────────────┤
│  OUTPUT  │ idea.md │ proposal.md │ code+verify │ delta specs │ …   │  5.72–7.02
└──────────┴───────────────────────────────────────────────────────────┘
```

关键字段：
- `index` / `tone` / `title` / `subtitle` / `tag`（+ `tag_solid`）：页头。
- `agents[]`：`{label, color, from, to, solid}`，`from`/`to` 是列下标（闭区间），
  `solid: true` 表示实色胶囊 + 白字，否则用 tint 底 + 同色字。
- `columns[]`：每列 = `flow` + `skills[]` + `cmds[]` + `deliv{title, desc[], dark}`。
  - 普通列：`flow = {title, sub, kind ∈ green|blue|dark}`
  - 块列：`flow = {block: true, stage, subpills[[中文, English], …], kind}`
    —— 子步骤**纵向堆叠**（横排在 10pt 下宽度不够）。
- `loops[]`：`{from, to, label, color}`，渲染成 FLOW 下方的虚线回路 + 左向箭头。
  多条回路按 `k` 逐行下移，**超过 2 条就会压到 SKILL 泳道**，请拆页。

**转写注意事项**（从 SVG 蓝图搬到 10pt 幻灯片时）：
1. **列宽必须拉平。** 原 SVG 用 78/310 这种悬殊列宽表达"主次"，
   但 78 单位 ≈ 0.72"，10pt 文字放不下。本模板一律等宽。
2. **标题里的中英文要拆开。** 原图 `title: '业务基线 Business'` 合在一起，
   11.5pt 下会折成两行。拆成 `title` + `sub` 两个字段。
3. **别删泳道。** 5 条泳道（含 SKILL/COMMAND）在 10pt 下仍然放得下，
   因为这两条泳道每列只有 1–4 个短词。删了反而丢失"谁执行"的信息。

---

### `layout: "capability-gap"`

适合：**"学得快、落地难"** 这类落差表达——把"上手成本"和"落地成本"画成两根长度悬殊的条。
示例：`assets/content.capability-gap.example.json`

```
┌──────────────────────────────────────────────────────────────────────┐
│ 核心实践：产品经理 / BA / SA 的两项硬要求                    28pt    │  0.28–0.88
│ 副标题 11pt                                                          │  0.94–1.24
├───────────────────────────────┬──────────────────────────────────────┤
│ ┌ 01 ┐ Skills                 │ ┌ 02 ┐ Git                          │  1.32
│ │ 能否熟练使用 …？             │ │ 能否熟练使用 …？                    │
│ │                              │ │                                    │
│ │ 上手  ▮▮             2 小时  │ │ 上手  ▮▮             2 小时        │  ← 0.5" 条
│ │ 落地  ▮▮▮▮▮▮▮▮▮▮▮▮▮▮  持续   │ │ 落地  ▮▮▮▮▮▮▮▮▮▮▮▮▮▮  持续         │  ← 3.1" 条
│ │ ─────────────────────────    │ │ ─────────────────────────          │
│ │ [难点] 养成习惯、规范化       │ │ [难点] 复杂大项目中的管理           │
│ │ ┌───────────────────────┐    │ │ ┌───────────────────────┐          │
│ │ │[办法] 组织纪律 + peer…│    │ │ │[办法] docs 独立 repo… │          │
│ │ └───────────────────────┘    │ │ └───────────────────────┘          │
│ └───────────────────────────────┴──────────────────────────────────────┘
│ ▌工具 2 小时就能学会，习惯和结构得靠组织长出来。▌                      │  6.48–6.98
└──────────────────────────────────────────────────────────────────────┘
```

关键字段：
- `cards[].tone`：决定编号 chip / 色条 / 办法底色的层色。
- `cards[].learn` / `cards[].land`：`{label, value, ratio}`。
  **`ratio` 是这一页唯一的可视化参数**——`0.16` vs `1.0` 就是"2 小时 vs 持续"。
  两边都用同一个 `TRACK_W`，读者靠条长对比直接读出落差。
- `cards[].question`（要求）/ `difficulty`（难点）/ `solution`（办法）。
- `thesis`：底部结论条。

**设计意图**：原稿把"2 小时就学会了"和"难点是养成习惯"写成两行并列的文字，
读者要自己比。这里把两者变成**两根长度差 6 倍的条**——
"上手很轻、落地很重"这个结论不需要读文字就能得到。

**适用边界**：只适合"同一件事的两个阶段成本悬殊"的场景。
如果两边成本差不多，画成条反而会暴露"其实没差别"，那就别用这个版式。

---

### `layout: "diagram-embed"`

适合：**主视觉由外部提供**的页面——你手里已经有一张画好的流程图/架构图，
只需要一个漂亮的外壳把它嵌进来。示例：`assets/content.diagram-embed.example.json`

```
┌──────────────────────────────────────────────────────────────────────┐
│ 扩展 SDD 实践：逐步尝试来如何支持                            28pt    │  0.28–0.88
├──────────────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────────────┐   │  1.00–2.00
│ │ [背景] 实践过 Harness 和 SDD 的伙伴…            11pt           │   │
│ │ [问题] 如何保持简单，以最易上手的方式…      13.5pt Bold 蓝字    │   │
│ └────────────────────────────────────────────────────────────────┘   │
│ ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐   │  2.16–4.26
│              [图片图标]                                             │
│         在此粘贴 OpenSpec 标准流程图                                 │
│         建议尺寸 12.49″ × 2.10″（约 5.9 : 1）                        │
│ └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘   │
├──────────────────────────────────────────────────────────────────────┤
│ 支持路径：从最小可用开始，逐步扩展                                    │  4.50
│  [L1·极简交付] ──▶ [L2·轻量产品] ──▶ [L3·业务产品]                   │  4.96–6.30
│  小型工具团队      小型业务产品团队    复杂业务产品团队               │
│  简单 · 轻治理                                   复杂 · 重治理        │  6.46
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓  ← 绿→板岩渐变   │  6.80
└──────────────────────────────────────────────────────────────────────┘
```

关键字段：
- `intro{context_label, context, question_label, question}`：**两行对照**——
  上行是"为什么难"（灰字，退后），下行是"所以要回答什么"（蓝字加粗，跳出）。
  原来是一整段 3 行文字，读者不知道重点在哪句。
- `placeholder{hint, note}`：**只画虚线框 + 图片图标 + 提示语**，不放任何真内容。
  换图时直接删掉 `px/py/pw/phh` 那块形状、贴图即可，其余版式不动。
- `progression.nodes[]`：`{tier, tone, team}`；箭头颜色取**下一个节点**的 `tone`，
  所以颜色是"指向哪里"而不是"从哪里来"。
- 底部渐变条用 `green → slate` 表示复杂度递增。

**设计意图**：外供图通常自带配色（比如深色流程图），硬套品牌色会打架。
所以这一页的策略是——**把外壳做干净（白底、灰字、虚线框），把彩色让给中间那张图**。
下方递进条用低饱和的品牌色收尾，不抢主视觉。

---

## 示意图组件（`pain-cards` 专用）

| `illustration` | 语义 | `illus` 参数 |
| :--- | :--- | :--- |
| `missing-agent` | 有疑问、缺工具 | `bubbles[]` `box` `box_sub` `caption` |
| `pipeline` | 多环节刚性流程 + 卡点 | `steps[]` `warning` `caption` |
| `docs` | 文档质量差、难沉淀 | `tag` `labels[]` `caption` |
| `roles` | 角色沿用旧方式 | `roles[]{name,initial}` `from` `to` `caption` |
| `gap` | 双方割裂、信息不对称 | `left_head` `left` `right_head` `right` `caption` |

新增示意图：写一个 `illus_xxx(slide, d, x, y, w, h)` 并注册进 `ILLUS` 字典即可，
`(x, y, w, h)` 是示意图面板的内框（约 2.11" × 2.20"）。

---

## Common Gotchas

1. **Keynote 会"抢"文档**（macOS 渲染最大的坑）
   Keynote 的 Resume 会把用户之前打开的 deck 一起恢复。如果脚本写
   `set theDoc to front document`，导出的会是**用户的 deck**，而你还以为渲染成功了。
   `render_slide.sh` 的做法：复制成唯一文件名 → 打开 → **按文档名定位** → 只导出/关闭这一个。
   永远不要 `close every document`。

2. **sips 只能转 PDF 第一页**
   本模板是单页，够用。多页请用 `pdftoppm`（poppler）或 LibreOffice 后端。

3. **中文字体要同时设 `latin` 和 `ea`**
   只设 `run.font.name` 不会作用于中文，必须写 `<a:ea typeface="...">`。
   `build_slide.py` 的 `_set_run_font()` 已经处理。

4. **圆角矩形 `adjustments[0]` 是相对 `min(w,h)` 的比例，不是绝对半径**
   想要 0.1" 圆角，值应该是 `0.1 / min(w, h)`。传错会让容器变成胶囊或完全方角。

5. **10pt 下限会强制你砍内容——这是设计意图，不是 bug**
   早期版本为了"忠实还原源图"把字号压到 6–7pt，结果投影上读不出来。
   现在 `MIN_PT` 会拦下任何小于 10pt 的 run；如果你发现内容放不下，
   正确处理是**减少条目 / 拆页 / 砍掉重复的表达**，而不是调低 `MIN_PT`。
   把 `--min-pt` 调小只应用于「打印稿」这类特殊场景，不要用于演讲投影。

6. **不要用 `…` 之外的占位符**
   看板里的 `…` 是"截断"语义，用 `gray_soft` 底 + `gray_mid` 字；不要用 "Lorem" / "TBD"。

---

## Files

| Path | 作用 |
| :--- | :--- |
| `scripts/extract_palette.py` | 图片 → 主色 + 占比 + 角色建议 + token JSON |
| `scripts/build_slide.py` | 数据驱动生成单页 16:9 PPTX，含几何自检 |
| `scripts/render_slide.sh` | PPTX → PNG（LibreOffice 优先，Keynote 兜底） |
| `assets/color-spec.md` | 「PPT 内容页配色要求」模板（可直接改来源复用） |
| `assets/palette.nidd-citylab.json` | 已抽取的 NiDD City Lab 成都站 token |
| `assets/content.example.json` | 内容建模示例（流程 + 对比看板） |
| `.env.example` | 可选配置（字体等），**无凭据** |

---

## First-Time Setup

```bash
pip install python-pptx Pillow
# 渲染（二选一）
brew install --cask libreoffice && brew install poppler   # 推荐
# 或：macOS 自带 Keynote + sips（无需安装，但有 Resume 坑，见 Gotchas）
```
